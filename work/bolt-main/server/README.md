# Bolt Server

A tiny Express server for local development.

- Start: `node index.js` (port 3001 by default)
- Endpoints:
  - `GET /api/hello` — simple hello message
  - `POST /api/contact` — echoes received JSON

Install deps: `npm --prefix server install`
